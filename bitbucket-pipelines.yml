clone:
  depth: 1
pipelines:
  default:
    - step:
        image: cloudrexx/web:PHP7.0-with-mysql
        script:
          - ls
          - pwd
          - mysql -h127.0.0.1 -uroot -pcloudrexx -e "SET @@SQL_MODE = CONCAT(@@SQL_MODE, ',NO_ENGINE_SUBSTITUTION');"
          - mysql -h127.0.0.1 -uroot -pcloudrexx < ./installer/data/contrexx_dump_structure.sql
          - mysql -h127.0.0.1 -uroot -pcloudrexx < ./installer/data/contrexx_dump_data.sql
          - ./cx wb test 2>&1
        services:
          - database
definitions:
  services:
    database:
      image: mariadb
      environment:
        MYSQL_ROOT_PASSWORD: 'cloudrexx'
        MYSQL_DATABASE: 'dev'